# --- App (NestJS / Prisma) ---
# Password special chars must be URL-encoded (@ → %40)
DATABASE_URL="postgresql://postgres:admin%40123@localhost:5432/Fusion_AI?schema=public"
JWT_SECRET="change-me-in-production"
JWT_EXPIRES_IN="7d"
PORT=3333
FRONTEND_URL="http://localhost:3000"

# --- Docker Compose Postgres 18 (used by docker-compose.yml at repo root) ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=admin@123
POSTGRES_DB=Fusion_AI
